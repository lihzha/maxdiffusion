# Master Experiment Tracker

Cross-experiment status index — one section per experiment/run, newest first. Updated at every handoff / wrap-up / pre-compact per the handoff protocol in `CLAUDE.md`. Per-experiment detail lives in `exp_<NN>_<name>_claude/` folders per `experiment_SOP.md`; this file is the at-a-glance map.

Last updated: 2026-08-07 ~05:30Z (model-change handoff: exp_06 section added at plan approval; the session-close addendum below carries exp_04/exp_05's final results and OVERRIDES their older section bodies; exp_02/03 sections owned by the parallel session, last refreshed 2026-08-04)

> **2026-08-07 session-close addendum (authoritative over the two section bodies below, which date from the J1-2 gate):** BOTH experiments' P1 phases are RESULTS-COMPLETE; reports (P4/P4') are the remaining work. **exp_05:** K1-2 SUCCEEDED — H1 PASS (own-basin SSIM 0.92/0.91, all examples improved) but H2 FAIL (fresh-noise 0.16, worse than control) and probe FAIL ⇒ **STOP both cohorts; Yixun honored the STOP — K2/K3 stand down permanently**; S1–S9+S10a all committed (suite 1417; S9 held open at the R14/R15 matrix stall, tripwired). **exp_04:** J1-4 SUCCEEDED (A0 0.67 / A1 0.85 / A1-probe 0.17 / A2 0.50 / A2-0 0.14 ⇒ STOP; A3 measurement ok) → J1b GO: joint endpoint optimization from fresh noise reaches own-basin quality on 3/8 (mean MSE 0.27 vs greedy 0.43) → R12 `transfer-probe` committed (989 tests) → J1c: **joint nulls retain 72% relative effect across basins (foreign SSIM 0.47 vs greedy's 0.17 collapse) but absolute floors unmet** — the mechanism arc (greedy artifact on both capacity AND transfer axes; remaining gap = optimization budget, not a basin wall) is COMPLETE; every predeclared gate has fired. Revival = an exp_06-scale proposal. Runbook lessons of the campaign: issues #10–#13 (classifier block, HyperParameters getattr + fix-propagation rule, xtrace leak, runbook idempotency under queue auto-retry — attempt-scoped roots are the standing pattern). Authoritative artifacts: `…/droid_wan_pos_context/k1/*`, `…/droid_wan_null_adapter/j1r2/*` (capacity/trainfit att-0806-164625, j1b_att-0806-211405, j1c_att-0807-020621). Job trail in each `_command.md`.

## exp_06 `rollout_adapter` — rollout-objective training of the frozen-backbone pre_context adapter (ACTIVE — plan v2.1 APPROVED 2026-08-07; code rounds T1–T7 opening)

- **What:** the campaign's convergence point, answering Yixun's project question directly ("a good action-conditioned world model for robotics WITHOUT fine-tuning the Wan2.2 backbone"). Train the **UNCHANGED** pre_context adapter (~128M, frozen 5B) with exp_03's **short-horizon differentiable rollout loss (R-B, k=2 primary)** instead of one-step denoising — the objective is the only manipulated variable. Motivated by: E1 per-clip optimized conditioning reaches 0.92 SSIM on the frozen backbone (exp_05 H1) so capacity is not the bottleneck; E2 one-step training is rollout-compounding-bottlenecked (exp_02's law) and greedy per-step optimization is basin-locked while through-the-sampler optimization transfers (exp_04 J1b/J1c); **E3 is the HYPOTHESIS** (amortizing per-clip full-horizon optimization into a state-conditioned adapter across clips × fresh basins) — a cross-slot/cross-horizon/cross-regime extrapolation, explicitly not pre-proven.
- **Gates (predeclared, as code):** primary = mean PAIRED per-example ΔSSIM(R-B − matched-C0) on DEV-64 **≥ +0.05 with paired-bootstrap CI-low > 0** (10k resamples, seed 20260804, coverage/imputation inherited), TEST-64 confirmation only after DEV passes, preceded by an anchor protocol (reproduce the historical 4-sample 0.2946, then freeze that checkpoint's DEV-64 benchmark row under the new paired protocol). **MANDATORY ACTION-USE GATE** (the reviewer's F3, the gate that makes this an *action-conditioned* claim): paired true vs **cohort-level seeded derangement** wrong-actions vs zero-actions on identical examples+noise, batch-1 executable, permutation+hash persisted; gate = mean paired ΔSSIM(true − wrong) CI-low > 0 on DEV, repeated on TEST; adapter-disabled row as diagnostic; the same battery runs on C0 (a finding either way).
- **Yixun's decisions (2026-08-07, verbatim "1–3 yes as recommended；4 defer"):** matched-C0 REQUIRED (same init/data order/seed stream/GBS/updates/optimizer/cadence/selection); pilot arm set = R-B k=2 + matched-C0 (k=4 exploratory only in an M1-measured cell; R-A/R-C deferred pending exp_03's S1.5/S1.6); M3 budget = **pilot 10k steps @ GBS 256**; compute-matched multi-draw one-step control DEFERRED.
- **Plan review:** 3 Codex passes — pass 1 REQUEST-REVISION (3 BLOCKER + 9 MAJOR, ALL accepted: trainer base is `WanTI2VSideAdapterTrainer` not S7's regression trainer; exp_03 dependency is a **pinned-SHA blob import + kernel extraction with equivalence tests** at `2ef9b8a`, NOT a branch merge — the reviewer measured ~44k insertions for a merge; the action-use gate; matched-C0 for causal claims; the paired gate form; the CFG gradient contract; support/PRNG/k policy; the DEV-64 fixed-draw selection estimand with TEST-64 structurally excluded; the exp_05-unstall claim withdrawn — exp_06's evaluator is `eval_wan_pos_rollout.py` so exp_05's S9 tripwire is untouched; cost honesty; per-SHA approvals) → pass 2 (10 RESOLVED, 2 pins) → **pass 3 APPROVE-PLAN**.
- **Key contracts for implementers:** CFG gradient contract — gradients through BOTH branches' dependence on the rollout state (no cross-step `stop_gradient` on z), adapter params only in the gradient tree, architecture's block-0 stop-grad preserved; proven by fixed-input parity with the deployed evaluator + a two-step central-finite-difference oracle (exp_04 R11's lesson: FD is the actual proof of differentiation). Support policy: one draw per batch, σ_hi uniform over grid indices {1..N−k}, terminal σ=0 never a start, fresh ε, resume/accumulation-stable PRNG folding; C0 and R-B share the batch stream. Eval/selection cadence: every 1,000 updates, both arms, YAML-declared and drift-pinned.
- **Rounds:** T1 `exp03-imports` (blobs + hashes + characterization) · T2 `loss-kernels` · T3a `cfg-rollout-step` · T3b `trainer-loop` · T4 `dispatch-config` · T5a `eval-anchor` · T5b `eval-gates` · T6 `launchers` · T7 `fit-probe-mode`. Jobs: M1 fit probe → M2 learnability (32 ex, ≤2k steps, numerical continuation rule) → M3 pilot → M4 eval; **each separately approved at its pushed SHA**.
- **Branch / worktree:** `claude-exp_06_rollout_adapter-20260807` (from exp_05's tip `0f505d3` — inherits exp_04's audited core + exp_05 S1–S10a; suite ~1417) @ `c07dc3f`; worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_06_rollout_adapter`. Docs: `exp_06_rollout_adapter_claude/` (plan v2.1, 3-pass review, worklog, query).

## exp_05 `pos_context` — positive text tokens from DDIM inversion + pre_context regression (ACTIVE — core through S4 committed; K1 gated only on the parity audit)

- **What:** Yixun's directive (given while approving exp_04): extract per-step **positive text embeddings** by DDIM inversion (`positive_inversion` role-swap of `embedding_search.py`) and train the **existing pre_context architecture** (~128M, unchanged) by **teacher-forced regression** onto those tokens — testing whether the deployed adapter's bottleneck was its training signal, not its capacity (its denoising-trained baseline: rollout SSIM 0.2946). Key convention (review-caught): tokens are optimized as the exact deployed representation — **8-token context passed directly as the whole `encoder_hidden_states`** (matches `wan_pre_context_adapter_forward`; reference L_pos forcing) — NOT inside a 512-row padded context; per-step optimization states `z̄_t` are cached for teacher-forced training. Load-bearing contract: the deployed path's bf16 activation cast (side_adapter_wan.py:767) is non-no-op — the runner-built `casting_velocity_fn` owns the cast (one rule), and fp16→bf16 double-rounding is NOT value-preserving (~6.25% of N(0,1) elements diverge), so record fidelity checks use feature tolerances.
- **Status (2026-08-05 ~19:00Z):** plan v3 **APPROVED**; merge-1 EXECUTED; rounds **S1–S4 ALL COMMITTED** through closed review cycles — HEAD `31739d4`, worktree clean, suite **1,063 tests** (219 exp_05-specific). Shipped: `models/wan/pos_context_inversion_wan.py` (POS_L=8, `truncate_or_pad_context`, `pos_context_from_t5`, `optimize_positive_embeddings` returning pos_embeds + per-step `z̄_t` states + z_final + [N,J,B] losses/grad-norms, `replay_with_positive` — plan MUST-1 discharged), `pos_context_records.py` (PosContextRecord + PosProvenanceHeader with `l_pos`/`embedding_slot`, `states_fidelity_check`; 7,468,516 B/record fp16 ⇒ 14.80 GiB DEV+TEST cohort), `pos_context_modes.py` (run_capacity_positive, pos_write_shard, pos_adoption, pos_selection_payload, normalized positive_roots), `configs/base_wan_5b_pos_inversion.yml`, dual-touch `run_wan_null_inversion.py` (+106/−6; null branch verbatim-preserved, pinned by test).
- **K1 gate:** conditionally granted (merge-1 ✓ + P0' green ✓ + J0 published ✓ + **parity audit — the one open condition, Planner writing it now**). Comparison videos ruled NOT K1-blocking (deferred to P4). After K1: S6–S10 (regression trainer, dispatch, evaluator, launchers), K2–K4, shared J5.
- **Inheritance:** binds to exp_04 plan v5 for manifests/noise/gates/integrity/verifier/legacy-restore contracts; same DEV-64/TEST-64/TRAINFIT-16/TRAIN-2000 cohorts (exp_04's published J0 manifests); jobs K1–K4 + shared J5. Pivots NOT shared with exp_04 (8-token inversion context).
- **Branch / worktree:** `claude-exp_05_pos_context-20260804` @ `31739d4` (clean, pushed); worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_05_pos_context`. Docs: `exp_05_pos_context_claude/` (plan v3, review trails, worklog, queries).

## exp_04 `null_adapter` — null-text inversion (Mokady) port + null-embedding adapter (ACTIVE — all rounds committed, J0 published, J1-2 relaunch READY awaiting Yixun's submit)

- **What:** Port null-text inversion to JAX against the frozen Wan2.2 TI2V 5B on the cached side-adapter latents: P1 capacity study + **noise-basin probe** (arms A0/A1/A1-probe/A2 fixed-ε₀/A2-0 control; A3 direct-opt in conditional J1b), P2 target caching (∅*_t ∈ [25,16,4096]), P3 a small (~9M) action-conditioned null predictor `(z_i0, actions) → ∅̂_t` — deployment needs NO modules inside the DiT (v_uncond gets the predicted null; v_cond stays frozen backbone + T5(""); w=5). Key context facts: side-adapter path uses T5("") in BOTH CFG branches; PyTorch fork never ran null inversion on DROID and its positive line died on the basin problem (own-z_init MSE 0.015 vs fresh-noise 2.7).
- **Status (2026-08-05 ~19:00Z):** **implementation COMPLETE for J1** — R1–R11 all committed through closed review cycles, plus the J1-crash fix round `hyperparameters-config-access` (commit `925ee17`; APPROVE first-pass, zero findings). Suite **954 tests**, ~470+ mutants killed cumulatively. **Parity audit CLEAN** (recorded per plan §8; worklog entry). Branch tip `9338c7b`, pushed.
- **J0 DONE:** J0-2 succeeded 2026-08-05 — cohort manifests published + immutable under `exp_04_null_adapter_claude/j0_manifests/` (VAL 14,636 records verified; TRAIN 5,000 episodes; 40 shards; listing checksum `5827f4da…0d14`). J0-1 FAILED-INFRA (TF/ADC `invalid_rapt`; ADC ≠ gsutil credentials — both logins needed).
- **J1:** attempt 1 (queue job `20260805-181744-61377ea2-exp04-j1-null-yixun`, tip `3616a94`) **FAILED — REAL BUG**: `getattr(config, "code_sha", "")` at run_wan_null_inversion.py:611 — HyperParameters raises ValueError, so three-arg getattr never falls back (pyconfig.py:316-319). Pipeline load succeeded first (validates the R10 backend). Fix: `optional_config_value` helper (get_keys → Mapping → getattr guarded on AttributeError+ValueError) for the 2 genuinely-optional sites; the 8 YAML-declared sites became loud direct reads; AST tests pin the split; regressions run against the real AST-extracted class; a second latent bug (entrypoint test fixture drifted from the YAML, hidden by getattr defaults) found + pinned. **J1-2 relaunch is READY at `9338c7b`** — grant conditions re-met (suite green, audit untouched by the delta); the local submit was blocked by the session's permission classifier (issue #10) ⇒ **awaiting Yixun to run `! bash <scratchpad>/submit_j1.sh`** (four-phase runbook: smoke → verify_replay → adequacy_probe → full capacity; verbatim J1-1 command, new tip).
- **After J1:** gates outcomes → target selection → J1b sizing (Yixun decision) → J2–J5, P3 adapter, P4 results/analysis/HTML reports.
- **Coder ops:** two agent retirements for repeated API drops (R6 handoff, exp_05-S2 handoff) + one honest context-limit refusal before R10-revision (fresh agent took it), all recorded; one persistent Coder per experiment since.
- **Branch / worktree:** `claude-exp_04_null_adapter-20260803` @ `9338c7b` (clean, pushed); worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_04_null_adapter`. Docs: `exp_04_null_adapter_claude/` (plan v5, 5-pass plan review, 12+ code-review files, worklog, `_command.md` J0-1/J0-2/J1-1, params, commits ledger, j0_manifests/).

## exp_03 `rollout_objective` — train on the trajectory the eval runs (ACTIVE — smokes closed, S1.5 probe in flight)

- **What:** exp_02 established that one-step denoising loss improves while 25-step rollout SSIM lags (the loss→SSIM law's slope is the compounding price). exp_03 attacks the objective/rollout mismatch directly with three trial losses on the same OVERFIT100 task: **A** = corrective scheduled sampling (k_A∼U{1,2} stop-grad advances along the descending σ grid via the extracted sampler, corrective target v\*=(z_lo−z_gt)/σ_lo — exact under the Euler rule); **B** = short-horizon differentiable rollout (k=2 sampler unroll, lax.scan + remat, endpoint MSE ÷ (σ_hi−σ_lo)²); **C** = literal same-batch λ·L_A+(1−λ)·L_B, λ=0.5, single Adam update. **Two tiers:** Tier 1 continues from exp_02's step-10,000 checkpoint (A/B/C vs the lr1e5c control, update-matched); Tier 2 retrains from step 0 (ctrl0 one-step control FIRST with an AND-gate replication of exp_02's exact RNG stream — |Δloss|≤1e-4, |ΔSSIM|≤5e-4 — then A0/B0/C0). Plan v3.2 **approved by Yixun**; primary metric = canonical seed-0 mean SSIM at the +0.02 practical-effect gate vs control.
- **Status: S2 TRAINING ARMS IN FLIGHT — the experiment proper is running.** All pre-training stages closed: S1 smoke, **S1.5** (dual-state discriminator data banked + reviewer-admissible), **S1.6** (at-scale budgets: A 1.17× PASS; B 2.713× over the 2.5× budget — Yixun ACCEPTED; C failed the GBS-256 fit by 34.32MB → Yixun chose **batch-2 + accumulation**).
  - **ctrl0 AND-GATE: PASSED AT 1e-11** — the exp_03 trainer reproduces exp_02 essentially bit-identically (losses 0.1919129606/0.1685259684/0.1459819537, deltas ~1e-11 vs a 1e-4 tolerance; SSIM@2500 0.8139005632, delta −2.5e-11). **The instrument is certified drift-free; every Tier-2 comparison is attributable to the objective.**
  - **Arms:** Tier 1 (from-10k, +2,500 updates, vs exp_02's lr1e5c control at the +0.02 gate): A `exp03-s2a-corrss-20260806`, B `…-rolloutl-…`, C `…-combined-…` (N=2 accumulation). Tier 2 (from init, 2,500): ctrl0 DONE, **B0 DONE** (eval pair launched), A0, C0. Seeds: per-arm byte-verified copies (23,654,557,930 B) of `wan-overfit100-s3-20260730/checkpoints/10000`.
  - **Gradient accumulation for C:** first implementation (unrolled + optimization_barrier) REGRESSED HBM to 31.98G on hardware — root cause **graph duplication** (a barrier orders execution, not allocation). Redesigned to `lax.scan` (body compiled once, accumulator as aliased carry, interleaved shard-local microbatches); reviewer APPROVE; **fit-smoke SUCCEEDED at GBS 256** — C runs at **5.92× control** (~2.3 h for Tier-1 C). GBS-128 fallback unused. Suite **1,584 passed / 2 skipped**.
  - **Two Planner launch defects, both caught and disclosed:** (1) the 10k seeds were copied to `wan-ti2v-overfit100/` while the exp03 launcher's Orbax root is `wan-ti2v-exp03/` → A/B silently trained from scratch (relaunched with explicit `OUTPUT_DIR`); (2) Tier-1 launches omitted `CHECKPOINT_STEPS`, so 12,500 was never saved and A's eval failed correctly (relaunched with `[10000,12500]`). Both cost compute, no data corruption. **Standing rule for exp_03 Tier-1 launches: pass BOTH `OUTPUT_DIR=…/wan-ti2v-overfit100` AND `CHECKPOINT_STEPS="[10000,12500]"` explicitly.**
  - **Fleet watcher** (`watchlist.txt` + a persistent monitor) polls every live job; routine failures (barrier SIGABRT/134, setup exits, preemptions) auto-resubmit with the new id appended to the watchlist; real defects get diagnosed instead.
- **Approval state:** Query-3/Query-4 conditional grants recorded in `rollout_objective_yixun_query.md` — S1 ✓ used, C re-smoke ✓ used, S1.5 ✓ in flight, **S1.6 granted** (gate-chained on S1.5; one-step mesh-fit at GBS 256 on v6e-64, also re-measures B/C budgets at scale). **S2a/S2b training arms are NOT yet approved** — a separate package goes to Yixun after S1.5/S1.6. Tier-1 launches must pass `EXP03_RAMP_ORIGIN=10000` explicitly (per-state ramp origins; Tier 2 uses 0).
- **Code:** suite **1,482 passed / 2 skipped** (grew from 1,007 this experiment). Key files: `models/wan/overfit100_sampling.py` (shared sampler), `trainers/wan_ti2v_exp03_trainer.py` (Exp03Trainer, EXP03_TI2V dispatch, control-by-identity, `exp03_aux_key` tracer-safe/resume-stable RNG + support-draw salt), parent hook `_make_train_step(loss_fn)` in the overfit100 trainer, `probe_exp03_s1_5.py` + launcher, `configs/base_wan_5b_exp03.yml`, `bash_scripts/train_wan_exp03.sh`, `diagnostics_exp03/`.
- **Branch / worktree:** `claude-exp_03_rollout_objective-20260802` (exp_02's branch merged in); worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_03_rollout_objective`. Docs auto-sync to `yixun-dev`.
- **Docs:** `exp_03_rollout_objective_claude/` — queries (grants verbatim), `plan_rollout_objective.md` v3.2, plan review (6 passes to APPROVE-PLAN), 5 code-review records all with strengthening records + final verdicts, `_command.md` (Jobs 1–8b with the S1 gate table and every disclosed launch error), worklog, commits ledger.
- **Next:** arm-by-arm eval pairs as runs land (instrument {10000 anchor, 12500} + canonical seed-0 SSIM for Tier 1; {250,1000,2500} + SSIM@2500 for Tier 2), then the primary-metric comparisons: Tier 1 vs lr1e5c's 0.9159 at +0.02; Tier 2 vs ctrl0 cell-for-cell. Open (non-blocking, next probe-touching round): S1.5's parity gradient-gap attribution + the trace_forward double-compile cause.

## exp_02 `overfit100` — text-conditioned 100-trajectory memorization test (FORMALLY CLOSED at 20,000 — verdict `partial`)

- **What:** Can full-FT Wan2.2 TI2V 5B **memorize** the 1,629 windows of **100 successful DROID trajectories** conditioned on per-trajectory language instruction (T5) + first-frame latent? Predeclared D11 criterion: canonical cohort (one window per episode, seed 0) mean-corr SSIM ≥ 0.95.
- **Verdict history — the headline moved twice:**
  1. **At LR 1e-5, step 10,000 (eval `46c5f41`): NOT ESTABLISHED** — canonical 0/100 ≥0.95 (mean 0.8416), full-set 229/1,629 ≥0.90. Practical plateau ≈0.84–0.85.
  2. **The plateau was an LR artifact.** User-approved LR sweep from the 10k checkpoint (2e-5, 5e-5, +1e-5 control, 2,500 steps each): 5e-5 cut one-step loss **27.5×** more than control. The analysis-review's H5 caveat (optimization floor, LR locked at 1e-5) was vindicated. For reference: Wan 2.1's own pretraining used LR 1e-4 (AdamW, wd 1e-3, plateau-triggered decay; Wan2.2 publishes no hyperparameters).
  3. **5e-5 extension arc (user-approved to 15,000, then 17,500):** canonical mean 0.8416 (10k) → 0.9159 (12.5k) → 0.9451 (15k) → **0.9508 (17,500)** (12.5k/15k values corrected in the v5.1 analysis review — earlier summaries had transcribed line-predictions); windows ≥0.95: **0 → 11 → 51 → 62/100**. The mean crossed the 0.95 bar at 17,500 — but the segment loss gains collapsed 0.0617 → 0.0213 → 0.0045 (per-2,500-step), a ~4.7× pace decay: an "echo plateau" at 5e-5.
- **The loss→SSIM law (the experiment's central quantitative finding):** canonical SSIM ≈ **0.9885 − 1.201 × (fixed-RNG one-step val loss)**, r = −0.9994, held on 5 independent checkpoints across three LRs — path-independent. Intercept sits above the 0.95 bar; the slope is the compounding price of the 25-step rollout. This is what exp_03 attacks.
- **Diagnostics (all user-approved, all closed):** D1 per-frame SSIM — frame-0 ≈0.97 uniformly, monotone decay over frames; pinning inflates the mean only +0.006. D2 fixed-RNG one-step loss instrument — deterministic (t,ε) per (episode, window); anchors bit-exact reproducible (0.12227@10k → 0.06061@12.5k → 0.03927@15k; 42σ final leg — not saturated). D3 episode-weighting hypothesis refuted (r=−0.09). Sampling probe: 50/100 rollout steps strictly worse than 25 (0/30 improved); no CFG branch exists in this rollout; REQUIRED_SAMPLING_STEPS=25 contract-locked.
- **The (a) probe ran and answered (2026-08-04):** 1e-4 from 17,500→20,000 (run `wan-overfit100-s3ext-lr1e4-20260804`, seeded by verified byte-identical copy). Anchor 0.0347633288 exact; 20,000 loss **0.03320** (gain −0.00156 ≈ ⅓ of the last 5e-5 segment) — **LR axis CLOSED, pace not restored, no divergence**. SSIM 20,000: mean **0.9536**, **67/100 ≥ 0.95**, law held a **6th** time (+0.0047 vs line). What remains to the 90/100 bar is the rollout-compounding price (exp_03's target).
- **FORMAL VERDICT (Jobs 50–51 + verdict CLI, 2026-08-04): `partial`.** Headline (canonical, median-of-3-seeds ≥ 0.95 for 90/100): **69/100** — not established (seeds 67/69/69, means 0.9536/0.9537/0.9536, validity vs Job 49 exact). Full-set: **99.32%** ≥ 0.90 (1,618/1,629, coverage complete) — numerically passes; two-tier claim withheld pending the headline, per the predeclared rule. Ablations: correct−null +0.0974, correct−shuffled +0.1233 (doubled from 10k; wrong context < no context). Arc of the statistic: 10k "none" (0/100; 14.1%) → 20k "partial" (69/100; 99.3%). Summary sentence: full-FT Wan2.2-5B *nearly* memorizes the 100 trajectories; the strict per-window 0.95 shortfall is the compounding rollout price (loss→SSIM law, 6 holds) — exp_03's objective. Verdict artifacts in `overfit100_s3_artifacts/`.
- **Code:** suite ~1,236 passed / 2 skipped at the exp_02 tip (exp_03's branch carries it to 1,482); ~30 Codex review passes. Fail-closed verdict pipeline (manifest cohorts, role-validated grids, whole-artifact admission, one-verdict-per-eval-commit) proved itself in production across preemptions.
- **Branch / worktree:** `claude-exp_02_overfit100-20260728`; worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_02_overfit100`. Docs auto-sync to `yixun-dev`.
- **GCS:** LR-sweep + extension run `wan-overfit100-s3ext-lr5e5-20260802` (checkpoints …12500/15000/17500 + validation dirs); base s3 run `wan-overfit100-s3-20260730` (250…10000 — also exp_03's Tier-1/S1.5 restore source).
- **Docs:** `exp_02_overfit100_claude/` — `_results.md` (full arc + decision menu), `_analysis.md` v4 (§5b diagnostics, two-lever synthesis), `_command.md` (Jobs 1–46, incl. disclosed errors), `overfit100_s3_artifacts/`, report HTML + generator.

## exp_01 `full_ft_overfit` — full-FT overfit diagnostic

- **What:** Plain Wan TI2V **full finetune** (backbone unfrozen, **no adapter**) overfit sanity check on DROID. Diagnostic to separate **(A)** "data/loss/pipeline broken" from **(B)** "frozen-backbone + adapter optimization too hard". Explicitly **NOT** the long-term method.
- **Status:** PARTS I+II COMPLETE (2026-07-27) — Part I: train-cohort SSIM 0.197→0.787 (official). Part II (held-out): val loss monotone 0.18447→0.17885 across ckpts 2500–20000 (n=14,636/pt, deterministic per-position (t,ε)); val-clip rollout SSIM 0.7269 vs train 0.7875 — domain transfer established, memorization share unquantified. Analyses Codex-reviewed (Part I: 6 findings; Part II: 6 findings — all applied). Reports 01/02/03/04 (03 = val gallery, 04 = train-cohort gallery; both with in-repo videos incl. |pred−GT| residuals ×4). **Merge decision made 2026-07-27: NOT merging** (Query 10) — code stays on the experiment branch; docs are on `yixun-dev` via the hook.
- **Branch / worktree:** `claude-exp_01_full_ft_overfit-20260715` off `yixun-dev` @ `8258965`; worktree `/Users/yixunhu/Home/maxdiffusion-worktrees/claude-exp_01_full_ft_overfit`. Exp-docs auto-sync to `yixun-dev` via `.githooks/post-commit`.
- **Docs:** `docs/worklogs_yixun/exp_01_full_ft_overfit_claude/` — queries 1–10, plan (Parts I+II), 2 plan reviews + 6 code reviews + 2 analysis reviews, worklog, `_command.md` (13 launches), params, results, analysis, reports 01–04 + assets.
- **Decision rule:** overfits fast → pipeline OK, continue adapter work; can't overfit after substantial steps → debug data / loss / noise / CFG / latent alignment first.
- **Next:** none open — exp_01 closed. Optional leftovers: qualitative video review by Yixun/Lihan; a matched held-out cohort job to quantify memorization; §2.4 controls (deferred, required only before any capacity/dose-ceiling claim).

## Run: wan-pre_context-v6e64-full-gbs512-fresh-20260629-034110

- **What:** `pre_context` adapter (~128M trainable) on frozen Wan2.2 TI2V 5B; DROID first-frame + 32×7 actions → video latents. v6e-64, global batch 512, pure FSDP, `side_adapter_noise_mode=fresh`, 30k steps.
- **Status:** COMPLETE (training + step-30000 validation).
- **Training health:** wandb train/val curves healthy, no overfit (checked 2026-07-11).
- **Validation @ step_030000** (25-step rollout, 4 samples, v6e-8 job — succeeded on attempt 6 after 5 infra preemptions):
  - mean_latent_mse **1.496**, mean_pixel_mse **0.0983**, mean_ssim **0.2946**
  - Artifacts: `gs://v6_east1d/checkpoints/maxdiffusion/wan-ti2v-pre-context-adapter/wan-pre_context-v6e64-full-gbs512-fresh-20260629-034110/validation/step_030000/` — per-sample `ground_truth.mp4` / `sample.mp4` / `comparison_gt_top_pred_bottom.mp4` / `metrics.json`, plus `summary.json`/`summary.csv`.
  - Interpretation: low SSIM does not contradict the healthy one-step denoising loss — the 25-step rollout accumulates error. User has the gsutil pull commands; awaiting their read on the videos.
- **Open options:** comparison validation at checkpoint 29000 (offered, not requested); next-experiment direction pending user's video review.

## Prior work (pre-tracker)

Side-adapter (~240M) and earlier pre-context history: see `docs/side_adaptor.md`, `docs/wan_ti2v_pre_context_adapter_methodology_results.md`, and Lihan's worklogs under `docs/worklogs/wan-ti2v-side-adapter/`.

## Process / infra state (for handoff)

- SOP: `docs/worklogs_yixun/experiment_SOP.md` — three-role separation (Planner Fable 5 / Coder Opus 5 max subagent, reused across rounds / Reviewer Codex `gpt-5.6-sol` xhigh), closed write→review→strengthen cycles, TDD tests in `src/maxdiffusion/tests/worklogs_yixun/`, every launch logged in `_command.md` at launch time.
- Codex reviewer: CLI 0.144.1, invoke `codex exec -s read-only -m gpt-5.6-sol -c model_reasoning_effort=xhigh "$(cat prompt)" < /dev/null` (the `< /dev/null` is mandatory — issue #5). Quota (issue #9): three accounts + two credit purchases consumed in one week; currently on account #3 (working as of 2026-08-05 ~19:00Z); budget ~2 passes per code round, ~5 for a from-zero plan review.
- TPU submissions from the Claude session can be blocked by the auto-mode permission classifier (issue #10, first hit 2026-08-05 on exp_04's J1-2): hand Yixun the submit script to run via the `!` prefix, or add a Bash permission rule for `tpu create`.
- Standing directives: **no TPU launch without Yixun's explicit prior approval** (announcement 02 — conditional grants recorded verbatim in query docs count); **Status block ends every reply** (announcement 01); auto-resubmit only for infra failures with unchanged code/config; user runs `gcloud auth login` themselves (yh4742@princeton.edu, recurs ~4-hourly — issue #6).
- Launch-shell hygiene (bit twice): zsh does NOT word-split unquoted vars — always inline or array-expand extra `tpu create` args; 2-minute Bash timeouts can kill submissions mid-flight (verify a job record exists after every submit); `gsutil -m` on macOS needs `-o "GSUtil:parallel_process_count=1"`.
- Submodule: `third_party/Wan2.2` = lihzha/Wan2.2 @ `f370228`.
- Handoff automation: `.claude/settings.local.json` + `.claude/hooks/handoff_snapshot.sh` (both gitignored, local-only) fire on `ConfigChange` / `PreCompact` / `SessionEnd` — breadcrumbs to `docs/worklogs_yixun/_handoff_events.log`. No hook exists for a model exhausting its quota; that trigger is proactive-only. Full protocol in `CLAUDE.md`.
- Branch: `yixun-dev` (integration). Experiments: exp_01 (COMPLETE, unmerged by decision), exp_02 (ACTIVE — decision menu), exp_03 (ACTIVE — S1.5 in flight, parallel session), exp_04 (**RESULTS-COMPLETE** — all P1/P1b/P1c gates fired; P4 report outstanding), exp_05 (**RESULTS-COMPLETE** — K1 STOP honored, K2/K3 stood down; P4' report outstanding), exp_06 (**ACTIVE** — plan v2.1 approved, T1 opening). Next experiment number is `exp_07`.
- **Monitoring pattern (learned 2026-08-06/07):** long-lived background shell pollers get killed by session teardown; use **ScheduleWakeup** for job monitoring instead (see issue #14). TPU submissions from the session are classifier-blocked (#10) — hand Yixun a `! bash <script>` line, or launch under an explicit in-conversation permission.
- **Report backlog:** exp_04 P4 and exp_05 P4' (results/analysis/HTML + comparison videos from the published `videos/` artifacts) are approved-in-principle work, not yet started — they do not block exp_06.
